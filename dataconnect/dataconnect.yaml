specVersion: "v1beta"
serviceId: "angular-firebase-messages"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-firebase-messages-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
